"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const lit_1 = require("lit");
const rule2G_1 = require("./rule2G");
describe('The function for rule 2G', () => {
    let container;
    beforeEach(() => {
        container = document.createElement('div');
        document.body.appendChild(container);
    });
    afterEach(() => {
        document.body.removeChild(container);
    });
    it('returns text content text nodes', () => {
        var _a;
        lit_1.render(lit_1.html ` <div id="foo">Hello world</div> `, container);
        const node = (_a = document.getElementById('foo')) === null || _a === void 0 ? void 0 : _a.childNodes[0];
        expect(rule2G_1.rule2G(node)).toBe('Hello world');
    });
    it('returns the empty string if no text content is present', () => {
        const node = document.createTextNode('');
        expect(rule2G_1.rule2G(node)).toBe('');
    });
    it('returns null if the node is not a text node', () => {
        lit_1.render(lit_1.html `
        <div id="foo">
          Hello world newline
        </div>
      `, container);
        const node = document.getElementById('foo');
        expect(rule2G_1.rule2G(node)).toBe(null);
    });
});
//# sourceMappingURL=rule2G_test.js.map