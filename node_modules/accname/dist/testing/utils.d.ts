/**
 * @license
 * Copyright 2020 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { RuleImpl } from '../lib/compute_text_alternative';
import { Context } from '../lib/context';
import { AccnameOptions } from '../lib/options';
declare type RuleRunner = (node: Node, context?: Context, options?: AccnameOptions) => string | null;
/**
 * Helper to abstract over the exact interface of a rule and avoid having to
 * explicitly pass a default context of the textAlternative function.
 */
export declare function createRuleRunner(rule: RuleImpl): RuleRunner;
export {};
//# sourceMappingURL=utils.d.ts.map